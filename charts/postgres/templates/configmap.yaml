{{- if .Values.initScript }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-init" (include "postgres.fullname" .) | trunc 63 | trimSuffix "-" }}
  labels:
    {{- include "postgres.labels" . | nindent 4 }}
data:
  init.sql: |
    {{- .Values.initScript | nindent 4 }}
{{- end }}
